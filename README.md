# 🍔 منصة المطاعم الذكية

منصة SaaS متكاملة لإدارة تطبيقات المطاعم

## 🚀 المميزات

- ✅ تطبيق ويب منفصل لكل مطعم
- ✅ لوحة تحكم كاملة
- ✅ إشعارات تلجرام فورية
- ✅ قاعدة بيانات PostgreSQL
- ✅ نظام مصادقة آمن
- ✅ API كامل

## 📦 التثبيت
```bash
npm install
```

## 🔧 الإعداد

1. انسخ `.env.example` إلى `.env`
2. املأ المتغيرات المطلوبة
3. شغّل `npm start`

## 🌐 API Endpoints

- `GET /` - الصفحة الرئيسية
- `GET /api/health` - فحص الحالة
- `GET /api/init-db` - تهيئة قاعدة البيانات
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/restaurants/register` - تسجيل مطعم جديد
- `GET /api/menu/:slug` - الحصول على المنيو
- `POST /api/orders/create` - إنشاء طلب

## 📝 License

MIT
